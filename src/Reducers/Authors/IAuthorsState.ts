/**
 * The properties needed to maintain the authors view.
 * @interface
 */
export interface IAuthorsState {
  /**
   *  name of the owner of the default repo.
   * @property
   */
  repoOwner: string;
}
